import { OpportunityList } from "@/components/OpportunityList";

export default function LearningPage() {
  return (
    <OpportunityList
      kind="course"
      title="Learning Center"
      subtitle="A leveled, ordered path through your AI focus — start the next module and tasks land in your weekly checklist."
    />
  );
}
