import { OpportunityList } from "@/components/OpportunityList";

export default function EventsPage() {
  return (
    <OpportunityList
      kind="event"
      title="Events, Hackathons & Networking"
      subtitle="Alberta + online AI events ranked by networking value, portfolio value, and urgency."
    />
  );
}
